import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sbox.chronicles.v2',
  appName: 'SuccessBOX 2.0',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
